<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.juanliu.captain" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.AddDemandActivity"
            android:label="@string/title_activity_add_demand" >
        </activity>
        <activity android:name=".activity.DemandDetailActivity">
        </activity>
        <activity android:name=".activity.MyDetailActivity"></activity>
        <activity android:name=".activity.MyDemandActivity"></activity>
        <activity android:name=".activity.SuperviseActivity"></activity>
    </application>

</manifest>
